{
  "name": "epoch",
  "version": "0.1.0",
  "private": true,
  "description": "Daily screenshot tracker — captures a list of URLs and stores them in this repo for later comparison.",
  "type": "module",
  "scripts": {
    "capture": "tsx scripts/capture.ts",
    "capture:dry": "tsx scripts/capture.ts --dry"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
